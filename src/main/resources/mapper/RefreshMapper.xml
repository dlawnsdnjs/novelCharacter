<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.novelcharacter.mapper.RefreshMapper">
    <insert id="insertRefresh" parameterType="RefreshDTO">
        INSERT INTO Refresh (uuid, refresh, expiration) VALUES (#{uuid}, #{refresh}, #{expiration});
    </insert>

    <select id="existsByRefresh" resultType="boolean" parameterType="string">
        SELECT EXISTS(SELECT * FROM Refresh WHERE refresh = #{refresh});
    </select>

    <delete id="deleteByRefresh" >
        DELETE FROM Refresh WHERE refresh = #{refresh};
    </delete>
</mapper>