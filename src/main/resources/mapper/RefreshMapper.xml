<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.novelcharacter.mapper.RefreshMapper">
    <insert id="insertStat" parameterType="RefreshDTO">
        INSERT INTO Refresh (
            name, refresh, expiration
        ) VALUES (
                     #{name}, #{refresh}, #{expiration}
                 );
    </insert>

    <select id="existsByRefresh" resultType="boolean" parameterType="string">
        SELECT EXISTS(SELECT * FROM Refresh WHERE refresh = #{refresh});
    </select>

    <delete id="deleteStat" >
        DELETE FROM Refresh WHERE refresg = #{refresh};
    </delete>
</mapper>