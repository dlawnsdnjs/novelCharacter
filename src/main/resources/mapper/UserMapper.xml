<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.novelcharacter.mapper.UsereMapper">
    <select id="selectUserById" resultType="UserDTO">
        SELECT * FROM User WHERE userId = #{userId};
    </select>

    <insert id="insertUser" useGeneratedKeys="true" parameterType="UserDTO">
        INSERT INTO Title (site, userId, userName) VALUES (#{site}, #{userId}, #{userName});
    </insert>

    <update id="updateUser" parameterType="UserDTO">
        UPDATE Title SET userName = #{userName} WHERE site = #{site} AND userId = #{userId};
    </update>

    <delete id="deleteUser" parameterType="UserDTO">
        DELETE FROM User WHERE site = #{site} AND userId = #{userId};
    </delete>
</mapper>