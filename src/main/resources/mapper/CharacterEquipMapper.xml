<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.novelcharacter.mapper.CharacterEquipMapper">
    <select id="selectCharacterEquipByIds" resultType="CharacterEquipDTO">
        SELECT * FROM CharacterEquip WHERE episodeNum = #{episodeNum} AND characterNum = #{characterNum} AND equipmentNum = #{equipmentNum};

    </select>

    <select id="selectCharacterEquipsByIds" resultType="CharacterEquipDTO">
        SELECT * FROM CharacterEquip WHERE episodeNum = #{episodeNum} AND characterNum = #{characterNum};
    </select>

    <insert id="insertCharacterEquip" parameterType="CharacterEquipDTO">
        INSERT INTO CharacterEquip (episodeNum, characterNum, equipmentNum) VALUES (#{episodeNum}, #{characterNum}, #{equipmentNum});
    </insert>

    <delete id="deleteCharacterEquip" parameterType="CharacterEquipDTO">
        DELETE FROM CharacterEquip WHERE episodeNum = #{episodeNum} AND characterNum = #{characterNum} AND equipmentNum = #{equipmentNum};
    </delete>
</mapper>